services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: strongfam
      POSTGRES_PASSWORD: strongfam
      POSTGRES_DB: strongfam
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
